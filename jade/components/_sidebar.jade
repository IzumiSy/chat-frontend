.profile-section
ul.room-list
  li.room-item(
    v-for="room in rooms"
    v-on:click="onRoomClicked(room)"
    v-bind:class="{ 'current': (currentRoomId == room.id) }"
  )
    span
      | {{ room.name }}
    span.badge
      | {{ room.users_count }}
